# Tools

Optional developer utilities. Keep this directory free of secrets.

Phase 0 contains no compiled tools. Future candidates:

- Fixture recorders (iRacing mmap → JSON) for `tests/fixtures`
- Protocol inspector for LAN captures
