# Protocol fixtures

Optional JSON documents for codec tests and future Bridge↔iOS contract tests.

Envelope schema: `packages/protocol/schemas/envelope.v1.json`.
C# round-trip tests currently embed JSON in `SimPulse.Protocol.Tests`.
