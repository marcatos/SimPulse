# BUG-001 handoff

## Task

BUG-001 — Pre-merge iRacing mmap review fixes for `feat/iracing-mmap-hr-windows`.

## Goal

Fix whole-branch Important findings before merge: Bridge must detect mmap after startup, decode session YAML as Latin1, and document car / change-detection limits.

## Status

DONE

## Files changed

- `apps/windows-bridge/SimPulse.Bridge.Core/Application/BridgeRuntime.cs`
- `apps/windows-bridge/SimPulse.Bridge.Core/Adapters/Iracing/IracingHeaderReader.cs`
- `apps/windows-bridge/SimPulse.Bridge.Core/Adapters/Iracing/WindowsIracingSharedMemory.cs`
- `apps/windows-bridge/SimPulse.Bridge.Core.Tests/BridgeCoreTests.cs`
- `apps/windows-bridge/SimPulse.Bridge.Core.Tests/IRacingAdapterTests.cs`
- `apps/windows-bridge/SimPulse.Bridge.Core.Tests/FakeIracingSharedMemory.cs`
- `apps/windows-bridge/SimPulse.Bridge.Core.Tests/IracingHeaderReaderTests.cs`
- `apps/windows-bridge/SimPulse.Bridge.Core.Tests/WindowsIracingSharedMemoryTests.cs`
- `docs/KNOWN_ISSUES.md`, `docs/handoffs/BRIDGE-003.md`, `docs/CURRENT_STATE.md`, `docs/BACKLOG.md`

## Decisions made

- Always enter `SubscribeAsync` after the startup availability check. `IRacingAdapter` already polls `TryOpen`.
- Decode IRSDK session info with `Encoding.Latin1` (Windows-1252 compatible for 0xFC `ü`; no code-page provider on Ubuntu CI).
- `WindowsIracingSharedMemory` reads header fields through `IracingHeaderReader.TryReadHeader`.
- TryOpen retries / misses log at Debug; Information only on opened / closed.
- Player car stays first `DriverInfo` YAML entry until `DriverCarIdx` telemetry (document only).
- Full `sessionInfoUpdate` change detection is a KI-002 follow-up (document only).
- ANALYTICS-003 `RaceReportBuilder` wiring is out of scope.

## Tests executed

- Focused: availability gate, delayed mmap, Latin1 `ü` (0xFC), header helper, TryOpen log levels
- `dotnet test SimPulse.sln --configuration Release`

## Tests passing

Yes. 73 passed, 0 failed (Domain 6, Analytics 9, Protocol 7, Bridge.Core 51). Apple suites NOT EXECUTED.

## Known failures

None on .NET. Apple suites NOT EXECUTED.

## Remaining work

None for BUG-001. Follow-ups stay on KI-002: `DriverCarIdx` player car, `sessionInfoUpdate` change detection, live smoke with iRacing.

## Risks

Fixture adapter with a missing path now throws from `SubscribeAsync` instead of idling (misconfiguration). Live iRacing still required for mmap smoke (KI-002).

## Suggested next action

Live smoke with iRacing + `irsdkEnableMem=1`, or BRIDGE-007 tray / pairing PIN UX.
