# BUG-003 handoff

## Task

BUG-003 — Pre-merge tray UX review fixes (round 2) for `feat/bridge-007-tray`.

## Goal

Stop file-log IO from crashing the Bridge host, log the pairing PIN only from the coordinator, and stop **Show current PIN** from advertising a closed pairing window.

## Status

DONE

## Files changed

- `apps/windows-bridge/SimPulse.Bridge.Core/Adapters/SimpleFileLoggerProvider.cs`
- `apps/windows-bridge/SimPulse.Bridge.Core/Adapters/ConsolePairingUx.cs`
- `apps/windows-bridge/SimPulse.Bridge.Core/Application/PairingCoordinator.cs`
- `apps/windows-bridge/SimPulse.Bridge.Core/Application/TrayPairingPresenter.cs`
- `apps/windows-bridge/SimPulse.Bridge.Core/Application/TrayPairingUxText.cs`
- `apps/windows-bridge/SimPulse.Bridge.Core/Ports/Ports.cs`
- `apps/windows-bridge/SimPulse.Bridge/Tray/NotifyIconPairingUx.cs`
- Core tests (file logger, presenter, ConsolePairingUx, UX text)
- `docs/DEVELOPMENT.md`, `docs/SECURITY.md`, `docs/KNOWN_ISSUES.md`, `docs/CURRENT_STATE.md`, `docs/BACKLOG.md`, `apps/windows-bridge/README.md`

## Decisions made

- File logger catches `IOException` / `UnauthorizedAccessException` / `NotSupportedException` on directory create and `AppendAllText`, disables further file writes, and does not throw.
- Coordinator keeps the existing Information `Pin=` line; UX `ShowPin` logs no longer include `Pin=`.
- `IPairingUx.ClearPin()` plus presenter consults `PairingCoordinator.IsPairingWindowOpen()` on **Show current PIN**. Consume/lockout raises `PairingWindowClosed` so the tray tooltip is cleared immediately. File logs contain the coordinator PIN — restrict log-directory ACLs.

## Tests executed

- Focused Core tests for file-logger write failure, ConsolePairingUx PIN-free ShowPin, presenter closed-window PIN
- `dotnet test SimPulse.sln --configuration Release`

## Tests passing

Yes. `dotnet test SimPulse.sln --configuration Release` — 102 passed, 0 failed (Domain 6, Analytics 9, Protocol 7, Bridge.Core 80).

## Known failures

None known. Ubuntu `dotnet test SimPulse.sln --configuration Release` was not executed on this Windows workstation; Core changes are `net8.0` with no WinForms.

## Remaining work

None for BUG-003. TLS remains under KI-003.

## Risks

Operators who can read `%LOCALAPPDATA%\SimPulse\logs` can recover the pairing PIN from the coordinator Information line. Live tray tooltip after consume was not smoke-tested on a hidden WinExe process in this session.

## Suggested next action

TLS for Bridge transport (KI-003 remaining).
