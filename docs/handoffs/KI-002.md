Task
KI-002 — Live iRacing mmap smoke

Goal
Verify the live IRSDK path on this PC: official mmap `Local\IRSDKMemMapFileName` opens, `irsdk_stConnected` is set, session YAML parses, and Bridge emits SessionStart plus lap events from a real sim (replay is sufficient).

Status
DONE — 2026-08-19 live replay smoke

Files changed
- `scripts/smoke-iracing-mmap.ps1`
- `docs/KNOWN_ISSUES.md`, `docs/CURRENT_STATE.md`, `docs/BACKLOG.md`, `docs/TESTING.md`, `docs/DEVELOPMENT.md`, `README.md`
- this handoff

Decisions made
- Replay is a valid live mmap source (same `iRacingSim64DX11` + `irsdkEnableMem=1`).
- No `SIMPULSE_FIXTURE_PATH`. File logs only (WinExe). Do not copy pairing PIN into git/Plane.
- Trusted-client broadcast stayed at Recipients=0 (no paired phone) — expected.
- ANALYTICS-003 RaceReportBuilder peak-event wiring remains out of scope.

Tests executed
- Manual: iRacing replay, mmap `status=1`, YAML ~43 KB, Bridge Information logs
- Evidence (redacted): mmap open succeeded; `Available=True`; `iRacing session started` YamlLength=43694; `SessionStart`; `LapStart` lap 1 `SessionNum=2`; later `LapComplete` lap 1 + `LapStart` lap 2
- `dotnet test` not re-run (no production code change)

Tests passing
N/A (docs + smoke script). Prior `main` suite still the KI-003 149-test baseline.

Known failures
None in the live path for session/lap events. 60 Hz WebSocket telemetry frames remain intentionally absent.

Remaining work
None for KI-002. Follow-ups: IOS-005 cert pin; KI-006 reconnect secret; optional trusted-client live broadcast with a paired device.

Risks
Mmap is absent when the sim is closed. CI must not require a live session.

Suggested next action
Merge this docs branch; next product work is IOS-005 or PROTO-003.
