Task
BUG-005 — SessionNum in lap keys + YAML decode cache

Goal
Stop `SessionLifecycleTracker` from dropping race LapStart/LapComplete 1 after SessionNum change, and stop Latin1-decoding the full session YAML blob on every 60 Hz snapshot when `sessionInfoUpdate` is unchanged.

Status
DONE

Files changed
- `apps/windows-bridge/SimPulse.Bridge.Core/Application/SessionLifecycleTracker.cs`
- `apps/windows-bridge/SimPulse.Bridge.Core.Tests/SessionLifecycleTrackerTests.cs`
- `apps/windows-bridge/SimPulse.Bridge.Core/Adapters/Iracing/IracingLiveSession.cs`
- `apps/windows-bridge/SimPulse.Bridge.Core.Tests/IRacingAdapterTelemetryTests.cs`
- `apps/windows-bridge/SimPulse.Bridge.Core/Adapters/Iracing/IracingMemorySnapshotReader.cs`
- `apps/windows-bridge/SimPulse.Bridge.Core/Adapters/Iracing/WindowsIracingSharedMemory.cs`
- `apps/windows-bridge/SimPulse.Bridge.Core.Tests/IracingMemorySnapshotReaderTests.cs`
- `docs/BACKLOG.md`, `docs/CURRENT_STATE.md`, `docs/KNOWN_ISSUES.md`, this handoff

Decisions made
- Lap events carry `sessionNum` (when observed) plus `lapNumber`. BuildKey is `{SessionId}:{Type}:{sessionNum}:{lapNumber}`.
- YAML string cache lives on an `IracingMemorySnapshotReader` instance owned by the open mmap (`WindowsIracingSharedMemory`). Unchanged `SessionInfoUpdate` reuses the cached string; telemetry is still read. Close/disconnect clears the cache.
- Static `TryRead` stays a one-shot helper (fresh instance, no persistent cache) for existing tests.

Tests executed
- Focused tracker / snapshot / SessionNum lap tests
- `dotnet test SimPulse.sln --configuration Release` (2026-08-18, Windows 10.0.26200, SDK 8.0.424)

Tests passing
129 passed, 0 failed (Domain 6, Analytics 9, Protocol 7, Bridge.Core 107). Apple/Xcode NOT EXECUTED. No live sim.

Known failures
None in unit tests. Live mmap path is untested on a running iRacing session (KI-002).

Remaining work
None for BUG-005. Live smoke (`irsdkEnableMem=1`) remains KI-002. Did not start TLS/Apple, torn-read retry, EventWaitHandle caching, or skip-RebuildSnapshot.

Risks
- CI uses Fake + synthetic buffers only.

Suggested next action
Live iRacing smoke (`irsdkEnableMem=1`) or TLS (KI-003).
