Task
IOS-004 — WatchConnectivity ingest

Goal
Receive Watch workout summary messages on iPhone via WatchConnectivity and merge idempotently by session ID (refresh session list / acknowledge ingest).

Status
IN_PROGRESS — branch `feat/watch-ios-sync-summary` (paired with WATCH-003)

Files changed
- (pending implementation)

Decisions made
- Paired delivery with WATCH-003; ingest against shared `WatchWorkoutSummaryMessage` contract.
- Idempotent merge by `sessionId` (= HealthKit workout UUID).
- See `docs/superpowers/specs/2026-08-19-watch-ios-summary-sync-design.md`.

Tests executed
- (pending)

Tests passing
- (pending)

Known failures
None yet.

Remaining work
- `WatchSummaryIngest` port + WC receive adapter
- Decode + validate schema version; ignore unknown versions (WARN)
- Trigger session list refresh after merge

Risks
- Duplicate delivery from WC retries — idempotency must hold.

Suggested next action
Implement iOS WC receive + ingest port per design spec (Task 3+).
