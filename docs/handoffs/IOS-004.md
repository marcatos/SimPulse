Task
IOS-004 — WatchConnectivity ingest

Goal
Receive Watch workout summary messages on iPhone via WatchConnectivity and merge idempotently by session ID (refresh session list / acknowledge ingest).

Status
DONE (on branch `feat/watch-ios-sync-summary`; pending PR/merge)

Files changed
- `apps/ios/SimPulse/Sync/WatchWorkoutSummaryMessage.swift` — shared wire DTO + userInfo encode/decode
- `apps/ios/SimPulse/Sync/WorkoutSummaryIngest.swift` — port + `UserDefaultsWorkoutSummaryIngest` + `.simpulseWorkoutSummaryMerged` notification
- `apps/ios/SimPulse/Sync/WatchConnectivitySummaryReceiver.swift` — WCSession activate, `didReceiveUserInfo` decode/validate/merge
- `apps/ios/SimPulse/SimPulseApp.swift` — retains receiver, calls `start()` in `init()`
- `apps/ios/SimPulse/Sessions/SessionListViewModel.swift` — observes merge notification → `load()`
- `apps/ios/SimPulseTests/WatchWorkoutSummarySyncTests.swift` — round-trip, schema, outbox, ingest idempotency, notification
- `SimPulse.xcodeproj/project.pbxproj` — regenerated on Mac (Task 4 verify)
- `docs/superpowers/specs/2026-08-19-watch-ios-summary-sync-design.md`
- `docs/superpowers/plans/2026-08-19-watch-ios-summary-sync.md`
- `docs/BACKLOG.md`, `docs/CURRENT_STATE.md`, `docs/handoffs/IOS-004.md`

Decisions made
- Paired delivery with WATCH-003; ingest against shared `WatchWorkoutSummaryMessage` contract.
- Idempotent merge by `sessionId` (= HealthKit workout UUID); duplicate delivery is no-op.
- Unknown schema version → WARN + ignore; malformed payload → ERROR (session ID logged only).
- Session list refresh via `NotificationCenter` (no UI chrome change).
- Screenshots: **N/A** — refresh-only ingest hook; no visible UI delta (see `.cursor/rules/apple-screenshots.mdc`).

Tests executed
- Mac (simpulse-mac, Xcode 26.6, iPhone 17 simulator): `xcodegen generate && bash scripts/test-ios.sh` — **31 passed**, 0 failed (2026-08-19, Task 4 verify).
- Mac build: `bash scripts/build-ios.sh` — **BUILD SUCCEEDED**; `bash scripts/build-watch.sh` — **BUILD SUCCEEDED**.
- Windows: NOT EXECUTED (expected).

Tests passing
Yes — 31/31 on iPhone 17 simulator:
- WatchWorkoutSummarySyncTests: 5 (round-trip, unknown schema, outbox idempotency, ingest duplicate, notification)
- Plus existing suite (HealthAuthorization 5, SessionDetail 7, SessionDetailViewModel 2, SessionRepository 4, glance 4, WorkoutSessionController 4)

Known failures
None.

Remaining work
- Open PR to `main` (paired with WATCH-003 on `feat/watch-ios-sync-summary`).
- Merge branch; optional paired-device WC delivery smoke.

Risks
- Duplicate delivery from WC retries — idempotency covered by unit tests; live retries not exercised on simulator.
- Ingest currently UserDefaults-only; SwiftData app-owned metadata deferred (ADR 0002).

Suggested next action
Open PR and merge `feat/watch-ios-sync-summary`; then Bridge KI-002/KI-003 or PROTO-003 Swift codec.
