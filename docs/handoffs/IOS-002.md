Task
IOS-002 — Session detail and charts

Goal
From the session list, open a session detail screen with duration, HR avg/max, active calories, and an HR-over-time Swift Charts line from stored HealthKit (or mock) samples via `SessionRepository.sessionDetail(id:)`.

Status
DONE (on branch `feat/ios-002-session-detail`; pending PR/merge)

Files changed
- `apps/ios/SimPulse/Sessions/SessionDetail.swift` — `HeartRatePoint`, `SessionDetail`, `HeartRateMetricsCalculator`
- `apps/ios/SimPulse/Sessions/SessionRepository.swift` — `sessionDetail(id:)` port + mock synthetic HR series
- `apps/ios/SimPulse/Sessions/HealthKitSessionRepository.swift` — workout lookup, HR sample query, metrics
- `apps/ios/SimPulse/Sessions/SessionDetailPresentation.swift` — formatted metrics + chart series
- `apps/ios/SimPulse/Sessions/SessionDetailViewModel.swift` — load by id, timing logs (no bpm)
- `apps/ios/SimPulse/Sessions/SessionDetailView.swift` — metrics header, Swift Charts `LineMark`, empty HR state
- `apps/ios/SimPulse/Sessions/SessionFormatting.swift` — shared duration/calorie formatters
- `apps/ios/SimPulse/Sessions/SessionListRowPresentation.swift` — row formatting reuse
- `apps/ios/SimPulse/Sessions/SessionListViewModel.swift` — `sessionsRepository` for navigation
- `apps/ios/SimPulse/SessionListView.swift` — `NavigationLink(value:)` + `navigationDestination`
- `apps/ios/SimPulseTests/SessionDetailTests.swift` — 7 contract/presentation tests
- `SimPulse.xcodeproj/project.pbxproj` — XcodeGen / manual pbxproj for new Swift files
- `docs/superpowers/specs/2026-08-19-ios-002-session-detail-design.md`
- `docs/superpowers/plans/2026-08-19-ios-002-session-detail.md`
- `docs/BACKLOG.md`, `docs/CURRENT_STATE.md`, `docs/handoffs/IOS-002.md`

Decisions made
- Extend `SessionRepository` with `sessionDetail(id:)` (hexagonal port).
- Swift Charts for HR-over-time; iOS 17+ deployment target.
- Avg/max HR as `Int?` from sample set via `HeartRateMetricsCalculator`; empty samples → nil metrics + `ContentUnavailableView` chart state.
- HealthKit: resolve workout by UUID, confirm Sim Racing metadata, query HR samples in workout interval; query errors → `nil` detail.
- Do not log HR bpm values or other biometric payloads.

Tests executed
- Mac (simpulse-mac, Xcode 26.6, iPhone 17 simulator): `xcodegen generate && bash scripts/test-ios.sh` — **24 passed**, 0 failed (2026-08-19).
- Mac build: `bash scripts/build-ios.sh` — **BUILD SUCCEEDED**.
- Windows: NOT EXECUTED (expected).

Tests passing
Yes — 24/24 on iPhone 17 simulator:
- HealthAuthorizationTests: 5
- SessionDetailTests: 7
- SessionRepositoryTests: 4
- WorkoutGlancePresentationTests: 4
- WorkoutSessionControllerTests: 4

Known failures
None.

Remaining work
- Open PR to `main` (`feat(ios): show session detail with heart rate chart`).
- Merge branch; verify chart UX on device with real HealthKit samples if desired.

Risks
- HealthKit read authorization may yield empty HR samples even when workouts exist (IOS-003 / KI-007 empty state covers list; detail shows honest empty chart).
- Device HealthKit / chart UX not exercised on simulator (expected).

Suggested next action
Open PR and merge `feat/ios-002-session-detail`; then WATCH-003 persist/sync or IOS-004 WatchConnectivity ingest, or Bridge KI-002 live mmap smoke.
