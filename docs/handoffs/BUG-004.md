Task
BUG-004 — Re-resolve iRacing session type from cached YAML

Goal
Keep YAML tokenization gated on `SessionInfoUpdate`, but re-run `IracingSessionInfoParser.Parse` on the cached YAML string when Available `DriverCarIdx` or `SessionNum` identity changes so SessionType/Vehicle update without a YAML tick.

Status
DONE

Files changed
- `apps/windows-bridge/SimPulse.Bridge.Core/Adapters/Iracing/IracingLiveSession.cs`
- `apps/windows-bridge/SimPulse.Bridge.Core.Tests/IRacingAdapterTelemetryTests.cs`
- `docs/handoffs/BUG-004.md`
- `docs/BACKLOG.md`
- `docs/CURRENT_STATE.md`
- `docs/KNOWN_ISSUES.md`

Decisions made
- Store `_cachedYaml` only when a YAML parse is accepted (first session + `SessionInfoUpdate` change).
- Identity-only refresh uses `_cachedYaml`, never the new snapshot YAML text.
- Available identity is `TryGet` or null; Unknown→Available and Available value changes both re-resolve.
- Does not take over BRIDGE-008 Task 4.

Tests executed
- `dotnet test ... --filter Session_num_change_with_same_update_resolves_session_type_from_cached_yaml` (RED then GREEN)
- `dotnet test SimPulse.sln --configuration Release`

Tests passing
126 passed, 0 failed (Domain 6, Analytics 9, Protocol 7, Bridge.Core 104). Apple/Xcode NOT EXECUTED.

Known failures
None.

Remaining work
Mark DONE after Release solution tests; do not close BRIDGE-008.

Risks
- Same-update mutated YAML must still keep the cached vehicle (existing test).
- Live sim still required for KI-002 end-to-end.

Suggested next action
Finish BRIDGE-008 Task 4 docs without reverting this identity re-resolve.
